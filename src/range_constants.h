#pragma once

namespace pdlfs {
static const size_t kMaxOobSize = 25;
static const size_t kMaxPivots = 256;
static const size_t kMaxPartSize = 256;
static const size_t kRenegInterval = 45000;
static const size_t kRtpPivotsStage1 = 8;
static const size_t kRtpPivotsStage2 = 8;
static const size_t kRtpPivotsStage3 = 8;
static const float kFloatCompThreshold = 1e-5;
}  // namespace pdlfs
